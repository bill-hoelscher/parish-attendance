services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: parish_attendance
      POSTGRES_USER: parish_app
      POSTGRES_PASSWORD: parish
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./db/migrations:/docker-entrypoint-initdb.d:ro

volumes:
  postgres_data:
